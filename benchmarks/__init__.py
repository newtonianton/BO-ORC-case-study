"""Multi-seed benchmark runner and result aggregator for the orc_bo pipelines.

Both entry points can be run either as modules (``python -m benchmarks.run_benchmark``,
``python -m benchmarks.aggregate_results``) or as plain scripts
(``python benchmarks/run_benchmark.py``).
"""
